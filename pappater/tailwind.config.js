/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
    darkMode: "class", // or 'media' for automatic dark mode based on OS settings
  },
  plugins: [],
};
